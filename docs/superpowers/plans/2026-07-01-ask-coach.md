# /ask Coach Conversacional — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Comando `/ask` no bot Telegram: conversa com o coach (Claude Sonnet) sobre uma corrida específica ou assunto geral, com contexto real (dados Garmin, readiness, nutrição), em thread contínua com botão finalizar.

**Architecture:** Reusa `src/ai_coach.py::ask_coach` (profile cacheado no system). Estende p/ aceitar histórico de mensagens. Fluxo stateful em `user_data` no padrão do `/comi`. Contexto montado por `bot/ask.py` (puro, testável). Handlers no `bot/handlers.py`, registro no `bot/main.py`.

**Tech Stack:** Python 3.11, python-telegram-bot, anthropic SDK, pytest.

## Global Constraints

- Sem `ANTHROPIC_API_KEY` (`bot_data["anthropic"]` é None) → `/ask` avisa e NÃO abre thread; bot segue no ar.
- Números/dados só de fonte real (Garmin/DB/nutrição). Nenhuma credencial Garmin vai ao Claude (profile não tem).
- TDD por tarefa, commits pequenos. Rodar testes com `python -m pytest`.
- Sem dependência nova.
- Estado da thread em `user_data` (memória do processo; some no restart — aceitável).
- Callbacks do `/ask` usam prefixo `ask:` (único, sem colisão com `nut:`/`dp:`/`ci:`/`act:`).

---

### Task 1: `ask_coach` aceita histórico de mensagens + Sonnet 5

**Files:**
- Modify: `src/ai_coach.py`
- Test: `tests/test_ai_coach.py`

**Interfaces:**
- Consumes: (nada novo)
- Produces: `ask_coach(messages: str | list[dict], context: dict, depth: str = "quick") -> str`
  — se `messages` é `str`, embrulha em `[{"role":"user","content": messages}]`; se é `list`,
  usa como está. `SONNET = "claude-sonnet-5"`.

- [ ] **Step 1: Write the failing test**

Adicionar ao fim de `tests/test_ai_coach.py`:

```python
def test_ask_coach_accepts_message_history(tmp_path, monkeypatch):
    _fake_profile(tmp_path, monkeypatch)
    fake_msg = MagicMock()
    fake_msg.content = [MagicMock(text="resp2")]
    fake_client = MagicMock()
    fake_client.messages.create.return_value = fake_msg
    history = [
        {"role": "user", "content": "primeira"},
        {"role": "assistant", "content": "ok"},
        {"role": "user", "content": "segunda"},
    ]
    with patch("src.ai_coach.anthropic.Anthropic", return_value=fake_client):
        out = ai_coach.ask_coach(history, {"hr": 50}, depth="deep")
    assert out == "resp2"
    sent = fake_client.messages.create.call_args[1]["messages"]
    assert sent == history
    assert fake_client.messages.create.call_args[1]["model"] == "claude-sonnet-5"


def test_ask_coach_string_still_works(tmp_path, monkeypatch):
    _fake_profile(tmp_path, monkeypatch)
    fake_msg = MagicMock()
    fake_msg.content = [MagicMock(text="r")]
    fake_client = MagicMock()
    fake_client.messages.create.return_value = fake_msg
    with patch("src.ai_coach.anthropic.Anthropic", return_value=fake_client):
        ai_coach.ask_coach("oi", {}, depth="quick")
    sent = fake_client.messages.create.call_args[1]["messages"]
    assert sent == [{"role": "user", "content": "oi"}]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_ai_coach.py::test_ask_coach_accepts_message_history tests/test_ai_coach.py::test_ask_coach_string_still_works -v`
Expected: FAIL (model é `claude-sonnet-4-6`; `messages` embrulha string sempre, quebra no history).

- [ ] **Step 3: Implement**

Em `src/ai_coach.py`, trocar a constante e a função:

```python
SONNET = "claude-sonnet-5"
```

```python
def ask_coach(messages, context: dict, depth: str = "quick") -> str:
    """
    messages: str (uma pergunta) OU list[{"role","content"}] (thread com histórico).
    depth='quick' → Haiku ; depth='deep' → Sonnet.
    """
    model = HAIKU if depth == "quick" else SONNET
    profile = _load_athlete_profile()

    profile_block = (
        f"Você é um coach de saúde e treino pessoal para {profile['nome']}.\n\n"
        f"PERFIL DO ATLETA:\n{json.dumps(profile, ensure_ascii=False, indent=2)}\n\n"
        "Responda em português. Seja direto e prático."
    )
    context_block = f"CONTEXTO ATUAL:\n{json.dumps(context, ensure_ascii=False, indent=2)}"
    system = [
        {"type": "text", "text": profile_block, "cache_control": {"type": "ephemeral"}},
        {"type": "text", "text": context_block},
    ]

    if isinstance(messages, str):
        messages = [{"role": "user", "content": messages}]

    client = anthropic.Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))
    message = client.messages.create(
        model=model,
        max_tokens=1024 if depth == "quick" else 2048,
        system=system,
        messages=messages,
    )
    return message.content[0].text
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_ai_coach.py -v`
Expected: PASS (todos, incluindo o antigo `test_ask_coach_system_is_block_list_with_cache_control`).

- [ ] **Step 5: Commit**

```bash
git add src/ai_coach.py tests/test_ai_coach.py
git commit -m "feat(ai_coach): aceita historico de mensagens e sobe pra Sonnet 5"
```

---

### Task 2: `bot/ask.py` — estado da thread

**Files:**
- Create: `bot/ask.py`
- Test: `tests/bot/test_ask.py`

**Interfaces:**
- Consumes: (nada)
- Produces:
  - `open_thread(user_data: dict, mode: str, run_id, context: dict) -> None`
  - `append_user(user_data: dict, text: str) -> None`
  - `append_assistant(user_data: dict, text: str) -> None`
  - `history(user_data: dict) -> list`
  - `get_context(user_data: dict) -> dict`
  - `is_active(user_data: dict) -> bool`
  - `close_thread(user_data: dict) -> None`
  - Estado guardado em `user_data["ask_thread"] = {"mode", "run_id", "context", "history": []}`.

- [ ] **Step 1: Write the failing test**

Criar `tests/bot/test_ask.py`:

```python
import bot.ask as ask


def test_open_and_active():
    ud = {}
    assert ask.is_active(ud) is False
    ask.open_thread(ud, mode="geral", run_id=None, context={"x": 1})
    assert ask.is_active(ud) is True
    assert ask.get_context(ud) == {"x": 1}
    assert ask.history(ud) == []


def test_append_roundtrip():
    ud = {}
    ask.open_thread(ud, mode="run", run_id=42, context={})
    ask.append_user(ud, "pergunta")
    ask.append_assistant(ud, "resposta")
    ask.append_user(ud, "de novo")
    assert ask.history(ud) == [
        {"role": "user", "content": "pergunta"},
        {"role": "assistant", "content": "resposta"},
        {"role": "user", "content": "de novo"},
    ]


def test_close():
    ud = {}
    ask.open_thread(ud, mode="geral", run_id=None, context={})
    ask.close_thread(ud)
    assert ask.is_active(ud) is False
    assert ask.history(ud) == []


def test_append_without_thread_noop():
    ud = {}
    ask.append_user(ud, "x")   # sem thread aberta: não quebra
    assert ask.is_active(ud) is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/bot/test_ask.py -v`
Expected: FAIL (`No module named 'bot.ask'`).

- [ ] **Step 3: Implement**

Criar `bot/ask.py` (só a parte de estado; contexto vem na Task 3):

```python
"""Estado e contexto da conversa /ask (coach). Puro, sem Telegram."""

_KEY = "ask_thread"


def open_thread(user_data, mode, run_id, context):
    user_data[_KEY] = {"mode": mode, "run_id": run_id,
                       "context": context or {}, "history": []}


def is_active(user_data) -> bool:
    return _KEY in user_data


def get_context(user_data) -> dict:
    return (user_data.get(_KEY) or {}).get("context", {})


def history(user_data) -> list:
    return (user_data.get(_KEY) or {}).get("history", [])


def append_user(user_data, text):
    th = user_data.get(_KEY)
    if th is not None:
        th["history"].append({"role": "user", "content": text})


def append_assistant(user_data, text):
    th = user_data.get(_KEY)
    if th is not None:
        th["history"].append({"role": "assistant", "content": text})


def close_thread(user_data):
    user_data.pop(_KEY, None)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/bot/test_ask.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add bot/ask.py tests/bot/test_ask.py
git commit -m "feat(ask): estado da thread do /ask em user_data"
```

---

### Task 3: `bot/ask.py` — montagem de contexto (corrida + geral)

**Files:**
- Modify: `bot/ask.py`
- Test: `tests/bot/test_ask.py`

**Interfaces:**
- Consumes: `build_run_detail(db, client, activity_id) -> {"activity","splits","insight"}`
  (de `src.services_core`); `today_panel(db_path, profile, date) -> {"today",...}`
  (de `bot.nutrition`); `db.get_snapshots(start, end) -> list`.
- Produces:
  - `build_run_context(db, client, activity_id) -> dict` → `{"activity","splits","insight"}`;
    se `build_run_detail` levantar (Garmin 429/sem splits), retorna `{"activity": <db>, "splits": [], "insight": None}`.
  - `build_general_context(db, db_path, profile, date) -> dict` → `{"readiness": <snapshot|{}>, "nutricao": <today|{}>}`.

- [ ] **Step 1: Write the failing test**

Adicionar ao fim de `tests/bot/test_ask.py`:

```python
from unittest.mock import MagicMock, patch


def test_build_run_context_ok():
    db = MagicMock()
    client = MagicMock()
    detail = {"activity": {"activity_id": 7, "name": "Corrida"},
              "splits": [{"km": 1}], "insight": "bom ritmo"}
    with patch("bot.ask.build_run_detail", return_value=detail):
        ctx = ask.build_run_context(db, client, 7)
    assert ctx["activity"]["activity_id"] == 7
    assert ctx["splits"] == [{"km": 1}]
    assert ctx["insight"] == "bom ritmo"


def test_build_run_context_degrada_sem_splits():
    db = MagicMock()
    db.get_activity.return_value = {"activity_id": 9, "name": "Corrida"}
    client = MagicMock()
    with patch("bot.ask.build_run_detail", side_effect=RuntimeError("429")):
        ctx = ask.build_run_context(db, client, 9)
    assert ctx["activity"]["activity_id"] == 9
    assert ctx["splits"] == []
    assert ctx["insight"] is None


def test_build_general_context():
    db = MagicMock()
    db.get_snapshots.return_value = [{"date": "2026-07-01", "resting_hr": 52}]
    panel = {"today": {"totals": {"kcal": 500}, "target": {"kcal": 1780}}}
    with patch("bot.ask.today_panel", return_value=panel):
        ctx = ask.build_general_context(db, "h.db", {"nome": "Yuri"}, "2026-07-01")
    assert ctx["readiness"]["resting_hr"] == 52
    assert ctx["nutricao"]["totals"]["kcal"] == 500


def test_build_general_context_sem_snapshot():
    db = MagicMock()
    db.get_snapshots.return_value = []
    with patch("bot.ask.today_panel", return_value={"today": {}}):
        ctx = ask.build_general_context(db, "h.db", {}, "2026-07-01")
    assert ctx["readiness"] == {}
    assert ctx["nutricao"] == {}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/bot/test_ask.py -k context -v`
Expected: FAIL (`build_run_context`/`build_general_context` não existem).

- [ ] **Step 3: Implement**

Adicionar imports no topo de `bot/ask.py` e as duas funções:

```python
from src.services_core import build_run_detail
from bot.nutrition import today_panel
```

```python
def build_run_context(db, client, activity_id) -> dict:
    """Contexto de uma corrida. Degrada sem splits se Garmin indisponível."""
    try:
        detail = build_run_detail(db, client, activity_id)
        return {"activity": detail["activity"], "splits": detail["splits"],
                "insight": detail["insight"]}
    except Exception:  # noqa: BLE001 — 429/sem splits: usa só o que tem no DB
        act = db.get_activity(activity_id)
        return {"activity": act, "splits": [], "insight": None}


def build_general_context(db, db_path, profile, date) -> dict:
    """Contexto do dia: readiness (snapshot determinístico) + painel de nutrição."""
    snaps = db.get_snapshots(date, date)
    readiness = snaps[0] if snaps else {}
    try:
        nutricao = today_panel(db_path, profile, date).get("today", {})
    except Exception:  # noqa: BLE001 — sem nutrição: contexto segue
        nutricao = {}
    return {"readiness": readiness, "nutricao": nutricao}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/bot/test_ask.py -v`
Expected: PASS (todos).

- [ ] **Step 5: Commit**

```bash
git add bot/ask.py tests/bot/test_ask.py
git commit -m "feat(ask): monta contexto de corrida e geral pro coach"
```

---

### Task 4: Handlers `cmd_ask` + `on_ask_button`

**Files:**
- Modify: `bot/handlers.py`
- Test: `tests/bot/test_ask_handlers.py`

**Interfaces:**
- Consumes: `bot.ask` (Task 2/3), `filter_runs`, `_run_button_label`, `_profile`,
  `bot_data["anthropic"]`, `bot_data["db"]`, `bot_data["db_path"]`, `bot_data["client"]`.
- Produces:
  - `async cmd_ask(update, context)` — sem `anthropic` → aviso; senão inline
    `[🏃 Corrida (ask:run)] [💬 Outro assunto (ask:geral)]`.
  - `async on_ask_button(update, context)` — trata `ask:run`, `ask:pick:<id>`,
    `ask:geral`, `ask:fim`.

- [ ] **Step 1: Write the failing test**

Criar `tests/bot/test_ask_handlers.py`:

```python
import asyncio
from unittest.mock import AsyncMock, MagicMock, patch
import bot.handlers as handlers
import bot.ask as ask


def _ctx(anthropic="client"):
    c = MagicMock()
    c.bot_data = {"anthropic": anthropic, "db": MagicMock(), "db_path": "h.db",
                  "client": MagicMock(), "cfg": MagicMock(chat_id=1)}
    c.user_data = {}
    return c


def _update(chat_id=1):
    u = MagicMock()
    u.effective_chat.id = chat_id
    u.message.reply_text = AsyncMock()
    return u


def test_cmd_ask_sem_api_key_avisa():
    u, c = _update(), _ctx(anthropic=None)
    asyncio.run(handlers.cmd_ask(u, c))
    txt = u.message.reply_text.call_args[0][0]
    assert "indispon" in txt.lower()
    assert ask.is_active(c.user_data) is False


def test_cmd_ask_com_api_mostra_botoes():
    u, c = _update(), _ctx()
    asyncio.run(handlers.cmd_ask(u, c))
    kb = u.message.reply_text.call_args[1]["reply_markup"]
    datas = [b.callback_data for row in kb.inline_keyboard for b in row]
    assert "ask:run" in datas and "ask:geral" in datas


def test_on_ask_geral_abre_thread():
    c = _ctx()
    q = MagicMock()
    q.data = "ask:geral"
    q.answer = AsyncMock()
    q.edit_message_text = AsyncMock()
    u = MagicMock(callback_query=q)
    with patch("bot.handlers.ask_build_general", return_value={"readiness": {}}):
        asyncio.run(handlers.on_ask_button(u, c))
    assert ask.is_active(c.user_data) is True
    assert ask.get_context(c.user_data) == {"readiness": {}}


def test_on_ask_fim_fecha_thread():
    c = _ctx()
    ask.open_thread(c.user_data, mode="geral", run_id=None, context={})
    q = MagicMock()
    q.data = "ask:fim"
    q.answer = AsyncMock()
    q.edit_message_text = AsyncMock()
    u = MagicMock(callback_query=q)
    asyncio.run(handlers.on_ask_button(u, c))
    assert ask.is_active(c.user_data) is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/bot/test_ask_handlers.py -v`
Expected: FAIL (`cmd_ask`/`on_ask_button`/`ask_build_general` não existem).

- [ ] **Step 3: Implement**

No topo de `bot/handlers.py`, adicionar imports (junto aos outros):

```python
import bot.ask as ask
from bot.ask import build_run_context as ask_build_run, build_general_context as ask_build_general
from src.ai_coach import ask_coach
```

Adicionar os handlers (perto de `cmd_macros`):

```python
_ASK_KB = InlineKeyboardMarkup([[
    InlineKeyboardButton("🏃 Corrida", callback_data="ask:run"),
    InlineKeyboardButton("💬 Outro assunto", callback_data="ask:geral"),
]])
_ASK_FIM_KB = InlineKeyboardMarkup([[
    InlineKeyboardButton("✋ finalizar", callback_data="ask:fim")]])


async def cmd_ask(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not _authorized(update, context):
        return
    if context.bot_data.get("anthropic") is None:
        await update.message.reply_text("🤖 Coach indisponível agora (sem ANTHROPIC_API_KEY).")
        return
    await update.message.reply_text("Sobre o quê? 👇", reply_markup=_ASK_KB)


async def on_ask_button(update, context):
    if not _authorized(update, context):
        return
    q = update.callback_query
    await q.answer()
    data = q.data
    if data == "ask:fim":
        ask.close_thread(context.user_data)
        await q.edit_message_text("Conversa encerrada. ✋ (/ask pra recomeçar)")
        return
    if data == "ask:geral":
        ctx = ask_build_general(context.bot_data["db"], context.bot_data["db_path"],
                                _profile(context), dt.date.today().isoformat())
        ask.open_thread(context.user_data, mode="geral", run_id=None, context=ctx)
        await q.edit_message_text("Manda tua pergunta 👇")
        return
    if data == "ask:run":
        client = context.bot_data["client"]
        try:
            runs = filter_runs(client.get_activities(7, fresh=True))[:8]
        except Exception:  # noqa: BLE001
            await q.edit_message_text("Não consegui buscar tuas corridas agora.")
            return
        if not runs:
            await q.edit_message_text("Nenhuma corrida recente encontrada.")
            return
        db = context.bot_data["db"]
        for r in runs:
            db.upsert_activity(activity_from_garmin(r))
        teclado = [[InlineKeyboardButton(_run_button_label(r),
                    callback_data=f"ask:pick:{r['activityId']}")] for r in runs]
        await q.edit_message_text("Qual corrida?", reply_markup=InlineKeyboardMarkup(teclado))
        return
    if data.startswith("ask:pick:"):
        activity_id = int(data.split(":")[-1])
        ctx = ask_build_run(context.bot_data["db"], context.bot_data["client"], activity_id)
        ask.open_thread(context.user_data, mode="run", run_id=activity_id, context=ctx)
        await q.edit_message_text("Manda tua pergunta sobre essa corrida 👇")
        return
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/bot/test_ask_handlers.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add bot/handlers.py tests/bot/test_ask_handlers.py
git commit -m "feat(ask): cmd_ask e on_ask_button (corrida/geral/finalizar)"
```

---

### Task 5: Roteamento da thread no texto + registro + ajuda

**Files:**
- Modify: `bot/handlers.py` (`on_text_macros`), `bot/main.py`
- Test: `tests/bot/test_ask_handlers.py`

**Interfaces:**
- Consumes: `ask.is_active`, `ask.history`, `ask.get_context`, `ask.append_user`,
  `ask.append_assistant`, `ask_coach`.
- Produces: turno da thread no `on_text_macros` (prioridade sobre macros);
  registro de `CommandHandler("ask")` e `CallbackQueryHandler(pattern=r"^ask:")`.

- [ ] **Step 1: Write the failing test**

Adicionar ao fim de `tests/bot/test_ask_handlers.py`:

```python
def test_text_thread_ativa_chama_coach():
    c = _ctx()
    ask.open_thread(c.user_data, mode="geral", run_id=None, context={"readiness": {}})
    u = _update()
    u.message.text = "é certo desacelerar no fim?"
    with patch("bot.handlers.ask_coach", return_value="depende do objetivo") as m:
        asyncio.run(handlers.on_text_macros(u, c))
    # histórico acumulou pergunta + resposta
    assert ask.history(c.user_data)[-2:] == [
        {"role": "user", "content": "é certo desacelerar no fim?"},
        {"role": "assistant", "content": "depende do objetivo"},
    ]
    # resposta enviada com botão finalizar
    kb = u.message.reply_text.call_args[1]["reply_markup"]
    assert kb.inline_keyboard[0][0].callback_data == "ask:fim"
    assert m.call_args[1]["depth"] == "deep"


def test_text_sem_thread_nao_chama_coach():
    c = _ctx()
    u = _update()
    u.message.text = "qualquer coisa"
    with patch("bot.handlers.ask_coach") as m:
        asyncio.run(handlers.on_text_macros(u, c))
    m.assert_not_called()


def test_text_thread_coach_falha_mantem_thread():
    c = _ctx()
    ask.open_thread(c.user_data, mode="geral", run_id=None, context={})
    u = _update()
    u.message.text = "pergunta"
    with patch("bot.handlers.ask_coach", side_effect=RuntimeError("timeout")):
        asyncio.run(handlers.on_text_macros(u, c))
    assert ask.is_active(c.user_data) is True   # thread segue aberta
    txt = u.message.reply_text.call_args[0][0]
    assert "tenta de novo" in txt.lower()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/bot/test_ask_handlers.py -k thread -v`
Expected: FAIL (thread não roteada em `on_text_macros`).

- [ ] **Step 3: Implement**

Em `bot/handlers.py`, no INÍCIO de `on_text_macros`, logo após o `if not _authorized(...)`,
inserir o roteamento da thread (antes de qualquer lógica de macros):

```python
    # thread /ask ativa tem prioridade sobre o fluxo de macros
    if ask.is_active(context.user_data):
        await _handle_ask_turn(update, context)
        return
```

E adicionar a função auxiliar (perto de `on_ask_button`):

```python
async def _handle_ask_turn(update, context):
    pergunta = update.message.text.strip()
    ask.append_user(context.user_data, pergunta)
    try:
        resp = ask_coach(ask.history(context.user_data),
                         ask.get_context(context.user_data), depth="deep")
    except Exception:  # noqa: BLE001 — mantém a thread aberta pra nova tentativa
        # remove a pergunta que não foi respondida do histórico
        context.user_data["ask_thread"]["history"].pop()
        await update.message.reply_text("Não consegui responder agora, tenta de novo.")
        return
    ask.append_assistant(context.user_data, resp)
    await update.message.reply_text(resp, reply_markup=_ASK_FIM_KB)
```

Em `bot/main.py`, registrar (junto aos outros handlers, ANTES do `MessageHandler` de texto):

```python
    app.add_handler(CommandHandler("ask", handlers.cmd_ask))
    app.add_handler(CallbackQueryHandler(handlers.on_ask_button, pattern=r"^ask:"))
```

E adicionar a linha no texto de ajuda do `/start` em `bot/handlers.py`
(dentro do `reply_text` de `cmd_start`, após a linha do `/macros`):

```python
        "/ask — conversar com o coach (corrida ou assunto geral)\n"
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/bot/test_ask_handlers.py tests/bot/test_ask.py -v`
Expected: PASS.

- [ ] **Step 5: Smoke de imports + suíte completa**

Run: `python -c "import bot.main, bot.handlers, bot.ask; print('OK')" && python -m pytest -q`
Expected: `OK` + suíte verde.

- [ ] **Step 6: Commit**

```bash
git add bot/handlers.py bot/main.py tests/bot/test_ask_handlers.py
git commit -m "feat(ask): roteia thread no texto, registra /ask, ajuda no /start"
```

---

## Deploy (após os 5 commits, quando o Yuri autorizar)

Mesmo processo das frentes anteriores (ver `garmin_nutricao` memory):
1. `cp ssh-key-2026-06-17.key /tmp/gk && chmod 600 /tmp/gk`.
2. Comparar md5 server vs parent (esperado: divergência só CRLF); tar dos arquivos com
   conteúdo LF de `git show HEAD:...`: `src/ai_coach.py bot/ask.py bot/handlers.py bot/main.py`
   + testes.
3. Backup tar no server, extrair, `.venv/bin/python -c "import bot.main"` + rodar
   `pytest tests/test_ai_coach.py tests/bot/test_ask.py tests/bot/test_ask_handlers.py`.
4. `sudo systemctl restart readiness-bot`, checar `is-active` + logs sem traceback.
5. Commit local no server; `shred -u /tmp/gk`.

Sem dependência nova, sem migração de DB. `athlete_profile.json` não muda.

## Self-Review

- **Cobertura do spec:** ask_coach histórico+Sonnet (T1) ✓; estado thread (T2) ✓;
  contexto run+geral com degradação (T3) ✓; cmd_ask sem-API + botões (T4) ✓;
  on_ask_button run/pick/geral/fim (T4) ✓; roteamento texto + prioridade + erro Sonnet
  mantém thread (T5) ✓; registro + ajuda (T5) ✓; deploy ✓.
- **Sem placeholders:** todos os steps têm código real.
- **Consistência de tipos:** `ask_coach(messages, context, depth)`, `open_thread(user_data,
  mode, run_id, context)`, chaves de contexto (`activity/splits/insight`,
  `readiness/nutricao`) batem entre tasks. Alias `ask_build_general`/`ask_build_run` usados
  no handler e mockados nos testes com o mesmo nome.
